const user = "/admin/v1";
export const USER = {
  LOGIN: `${user}/login`,
};

const kelas = "kelas/v1";
export const KELAS = {
  ALL: `${kelas}/all`,
};

const siswa = "siswa/v1";
export const SISWA = {
  ALL: `${siswa}/all-siswa`,
  PER_KELAS: `${siswa}/all`,
};
